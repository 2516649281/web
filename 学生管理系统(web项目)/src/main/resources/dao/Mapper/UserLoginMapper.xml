<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD dao.Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.Mapper.UserLoginMapper">
    <insert id="AddUserLogin">
        insert into user_login(username, login_time, end_time)
        values (#{UserName}, #{LoginTime}, #{EndTime});
    </insert>
</mapper>